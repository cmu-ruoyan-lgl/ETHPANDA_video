要求：将“优化后的英文字幕”转换为“中英对照”格式（每块两行：中文在上 + 英文在下），翻译时严格参考 `docs/WEB3专有名词翻译.md` 中的术语规范。

### 功能说明
- 保留 SRT 的编号与时间轴行，不改变时间戳。
- 将每个字幕块的英文文本合并为一行，并生成对应中文行（两行结构更利于播放器展示）。
- 翻译前对术语做占位保护，翻译后按 `docs/WEB3专有名词翻译.md` 替换为规范中文，确保术语一致性。

### 一键处理“subtitles_en_优化后/”下所有小字幕（交给 Agent）
在本项目根目录中，请让 Agent 批量执行以下需求：
- 将 `./subtitles_en_optimized/` 下所有 `.srt` 文件转换为“中英对照”格式；
- 行序为：第3行中文（合并为一行），第4行英文（合并为一行）；
- 保留编号与时间轴行，绝不改动时间戳；
- 翻译前对术语占位保护，翻译后按 `docs/WEB3专有名词翻译.md` 替换为规范中文，确保一致；
- 输出目录：`./subtitles_bilingual/`，文件名与源文件一致；如不存在则创建；
- 若后续需要原位覆盖，再单独说明“覆盖写回”。

处理结果输出到 `subtitles_bilingual/`，与原文件同名，SRT 块结构为：
- 第1行：编号（不变）
- 第2行：时间轴（不变）
- 第3行：中文（按术语规范翻译）
- 第4行：英文（合并为一行）

### 仅处理单个文件（交给 Agent 的示例）
请让 Agent 执行：
- 输入：`./subtitles_en_optimized/xxx.part01.srt`
- 输出目录：`./subtitles_bilingual/`
- 规则：中文在上、英文在下；保留编号与时间轴；术语按 `docs/WEB3专有名词翻译.md`

### 备注
- 术语表默认位于 `docs/WEB3专有名词翻译.md`，如需临时补充，可直接更新该文件。
- 若中文行过长，建议后续在“发布前检查”步骤中人工回看必要的换行与断句。
- 默认写入 `subtitles_bilingual/`。如需原位覆盖，请明确说明该需求后再执行（谨慎）。
