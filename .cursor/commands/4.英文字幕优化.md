批量审校“已切分”的英文 SRT：提升语言逻辑与可读性，去除口头语与不流畅部分，保持原意与 SRT 结构不变，并统一常见 Web3 术语写法（如 Web3、DeFi、NFT、PoS/PoW、ZK Rollups 等）。

### 特点
- **跨块上下文衔接**：字幕可能将同一句分布在不同时间段或分块中，处理与翻译时需结合前后字幕，确保语义连贯、指代清晰、术语一致。
- **严格保留** 字幕编号与时间轴行（不会改动时间戳）；**仅改写文本行**。
- **移除口头语和赘述**（um/uh/you know/I mean/…），在不改变语义的前提下让表达更地道、流畅。
- **统一 Web3 术语写法与大小写**（case-insensitive），如 Web3/DeFi/NFT/DAO/EVM/PoS/PoW/Layer 2/ZK Rollups/TVL/TPS/RWA/LSD/LST 等。
- **保留原有换行与块结构**，不会破坏 SRT 格式。

### 约束（必须遵守）
- 进行手动审校：不运行任何脚本/命令，不依赖任何自动化工具。
- 全程由助手执行，不向用户分派任何手工操作或命令行步骤。
- 不新增或修改脚本文件；严禁临时创建后再删除脚本；不得以任何形式引入自动化配置/宏/任务编排；仓库结构保持不变。需要输出结果时，由助手在不破坏结构的前提下新建 `subtitles_en_optimized/` 目录与结果文件。
- 如发现路径缺失或文件不在预期位置，请停止并提示，不创建脚本或自动化配置。

### 沟通约定
- 默认由助手批量完成 `subtitles/` 下全部 `.srt` 审校与保存，无需用户逐一指派。
- 仅在以下情况暂停并请示：文件/目录缺失、无法判定的专有名词、与项目词表冲突、需要新增目录结构。
- 交付时提供简要说明与抽样片段，且不附带任何脚本或命令。

### 助手执行流程（批量/单文件通用）
- 扫描 `subtitles/` 目录，列出全部 `.srt` 清单（不改动仓库结构）。
- 逐块仅改写“文本行”，严格保留编号与时间轴行不变。
- 结合上下文处理跨块语义与指代，统一术语与大小写。
- 移除口头语与不流畅表达，优化语序但不改变事实与数值。
- 保存为 `subtitles_en_optimized/<同名>.srt`；若目录不存在，由助手新建。

### 执行范围与输出
- 输入目录：`./subtitles` 下全部 `.srt` 文件。
- 输出目录：`./subtitles_en_optimized`，文件与输入同名。
- 处理节奏：边处理边保存，避免一次性大范围改动难以回溯。

### 单文件处理（按需）
- 输入：`./subtitles/<文件名>.srt`
- 输出：`./subtitles_en_optimized/<文件名>.srt`（由助手完成保存与校验）

### 覆盖原文件（可选，谨慎）
如需原位覆盖，由助手在备份后将审校后的文本行替换原文件对应文本行；编号与时间轴保持不变。

### 质量校验清单（助手自检）
- 结构一致：块数、编号、时间轴与原文件完全一致；仅“文本行”发生变更。
- 语义连贯：跨块衔接自然，无指代歧义，不破坏上下文逻辑。
- 术语一致：Web3/DeFi/NFT/DAO/EVM/PoS/PoW/Layer 2/ZK Rollups/TVL/TPS/RWA/LSD/LST 等写法统一。
- 专名与数值：人名、机构名、代号、年份/数值不被更改或误改。
- 语言质量：语法与拼写正确，标点规范；不强制重排行宽与换行，保留原块结构。
- 抽检与回看：每文件随机抽检 ≥5% 块进行二次复核，必要时回修全片一致性。
- 异常处理：若发现时间戳错位、重复编号或明显缺字断句，暂停该文件并提示后再处理。

### 说明与建议
- 审校仅改写“文本行”，**绝不修改编号与时间轴**；并保持块结构与换行。
- Web3 术语采用常见写法（Web3/DeFi/NFT/DAO/EVM/PoS/PoW/Layer 2/ZK Rollups/TVL/TPS/RWA/LSD/LST 等），并尽量保持全片一致。
- 后续如需生成双语字幕，请使用 “/5.翻译成中英文对照字幕”，并结合 `docs/WEB3专有名词翻译.md` 做术语一致性核对（仍遵守本条的“纯人工、禁脚本/命令/自动化”的原则）。

